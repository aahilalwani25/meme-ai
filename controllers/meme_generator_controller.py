from PIL import Image, ImageDraw, ImageFont
import textwrap

class MemeGeneratorController:

    def generate_meme_text(template: Image):
        # Generate random text for the top and bottom of a meme.
        draw= ImageDraw.Draw(template)
        image_width, image_height= template.size
        pass

    def create_meme(text):
        # Create a new image based on the provided template with the specified top and bottom texts.
        pass
